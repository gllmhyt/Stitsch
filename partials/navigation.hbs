<nav id="nav">
	<div class="mobileoff">
		{{#foreach navigation}}
			<a class="nav-{{slug}}{{#if current}} nav-current{{/if}}" href="{{url absolute="true"}}"><span>{{label}}</span></a>
		{{/foreach}}

        <!-- <span class="subnav-title" href="#">Que lire ? <span class="oi" data-glyph="caret-bottom"></span>
            <div class="subnav">
                <a href="#"><span>idées</span></a>
                <a href="#"><span>humeurs</span></a>
                <a href="#"><span>fictions</span></a>
                <a href="#"><span>divertissement</span></a>
                <a href="#"><span>inclassable</span></a>
            </div>
        </span> -->
        <a href="{{twitter_url @blog.twitter}}" title="Twitter"><span class="fa fa-twitter"></span></a>
        <a href="{{facebook_url @blog.facebook}}" title="Facebook"><span class="fa fa-facebook"></span></a>
        <a href="{{@blog.url}}/rss/" title="Flux RSS"><span class="fa fa-rss"></span></a>
        <a href="{{@blog.url}}/ghost/" title="Administration"><span class="fa fa-pencil"></span></a>
	</div>
    
	<select class="custom nolabel mobileon" name="Menu" onchange="location = this.options[this.selectedIndex].value;">
		<option value="#" selected="selected">Menu</option>
		{{#foreach navigation}}
			<option value="{{url absolute="true"}}">{{label}}</option>
		{{/foreach}}
	</select>
</nav>
